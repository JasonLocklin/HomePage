#!/bin/sh
# send-webmentions.sh — Send outgoing webmentions for published posts
#
# Future project: detect changed posts (via git diff or a timestamp file),
# extract outgoing links, and notify each target via the Webmention protocol.
#
# Suggested tool: https://webmention.app or the `webmention` CLI.
# See: https://www.w3.org/TR/webmention/ for the spec.

echo "send-webmentions: not yet implemented"
